import { LoginForm, RegisterForm } from "../types/types"

export const DEFAULT_LOGIN_STATE: LoginForm = {
    email: '',
    password: ''
}

export const DEFAULT_REGISTER_STATE: RegisterForm = {
    email: '',
    password: '',
    name: '',
    img: ''
}